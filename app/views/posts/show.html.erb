<p id="notice"><%= notice %></p>
<h1><%= @post.title %></h1>
<p>
  <strong>User:</strong>
  <%= @post.user.name %>
</p>

<p>
  <strong>Orders:</strong>
  <% @post.orders.each do |o| %>
    <li><%= o.drink.name %> | <%= o.quantity %>
  <% end %>
</p>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
